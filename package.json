{
    "name": "rgt-ai-agent-user-guide",
    "version": "1.0.0",
    "scripts": {
      "build": "mintlify export --output export.zip && unzip -o export.zip -d docs-build",
      "dev": "mintlify dev"
    },
    "dependencies": {
      "mintlify": "latest"
    },
    "engines": {
      "node": ">=20.17.0"
    }
  }